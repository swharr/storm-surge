# Core Flask web framework and server
flask==2.3.3
gunicorn==21.2.0

# HTTP requests and API communication
requests==2.31.0

# JWT token handling for authentication
PyJWT==2.8.0

# YAML parsing for configuration
pyyaml==6.0.1

# Feature flag provider SDKs (conditionally imported)
# LaunchDarkly SDK (optional - only if using LaunchDarkly)
# launchdarkly-server-sdk==8.2.1

# Statsig SDK (optional - only if using Statsig)
# statsig==1.20.0

# WebSocket support for real-time features
flask-socketio==5.3.6
python-socketio==5.8.0

# CORS support for cross-origin requests
flask-cors==4.0.0

# Password hashing
bcrypt==4.0.1

# Rate limiting
Flask-Limiter==3.7.0
